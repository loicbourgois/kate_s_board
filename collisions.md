# Collisions

- reset grid
- assign node to grid cell
- for each node(n1)
    - for each grid cell close to n1
        - for each node (n2) in the grid cell
            - detect collision
- treat collisions
